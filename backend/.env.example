# Install Dotenvx: curl -fsS https://dotenvx.sh | sh
# Add your Gemini API key to .env.
# Encrypt .env: dotenvx encrypt (do not push .env.keys).
# Run with env decryption: dotenvx run cargo run.

GEMINI_API_KEY=